# Instruction :

## Create python venv, install requirements.txt
## start server with "python main_with_logging"
## start ngrok reverse proxy with command "ngrok http 5050"
## In twilio console add the ngrok url with the /incoming-call endpoint
## Call the twilio number, and done!
# Find recording in log
